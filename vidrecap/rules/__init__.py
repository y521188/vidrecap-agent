"""规则层：标准与宪法。

本层管三类东西：
1. 判断标准——质量打分规则、阈值闸门、忠实性护栏（待开发，见路线图）；
2. 项目宪法——仓库根目录的 AGENTS.md 与 docs/ARCHITECTURE.md 由本层负责维护；
3. 规则的执行边界——tests/test_layering.py 把分层规矩变成 CI 硬检查。

与规划层一样，本层只做纯判断：同样输入永远同样输出，不读时钟、不用随机、
不做 I/O（由 tests/test_layering.py 强制）。
"""
